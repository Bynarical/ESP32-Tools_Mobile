{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true
  },
  "//": "tests/ runs in node and web/ runs in a browser, neither of which is React Native, so each is typechecked by its own config - tsconfig.test.json and web/tsconfig.json. Node and DOM globals do not exist here.",
  "exclude": ["tests", "web", ".test-build", "node_modules"]
}
